Use the resume-writer agent to create a tailored resume for the following job posting:

$ARGUMENTS

Read knowledge_base/candidate_profile.yaml and knowledge_base/candidate_narrative.md first. Check config/resume_style.yaml for formatting preferences. Output a .docx file to output/resumes/ following the naming convention: resume_[company]_[role]_[YYYY-MM-DD].docx. Report minimum qualification coverage and any gaps.
